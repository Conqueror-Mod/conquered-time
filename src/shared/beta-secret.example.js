'use strict';
// TEMPLATE for src/shared/beta-secret.js (the real file is gitignored).
// Generate your own with:
//   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
// then copy this file to beta-secret.js and paste the value below.
module.exports = 'REPLACE_WITH_A_RANDOM_32_BYTE_HEX_SECRET';
